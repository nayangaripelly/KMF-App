// Force light theme across the app to match the provided design
// If you want to respect system theme later, switch back to exporting from react-native
export function useColorScheme(): 'light' {
  return 'light';
}
